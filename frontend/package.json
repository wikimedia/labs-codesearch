{
	"private": true,
	"scripts": {
		"lint": "eslint --cache --format tap .",
		"fix": "eslint --cache --fix .",
		"test": "npm run lint"
	},
	"devDependencies": {
		"eslint-config-wikimedia": "0.24.0"
	}
}
